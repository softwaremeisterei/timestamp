# TIMESTAMP - Rename a file by adding a timestamp (Windows)

      Usage: timestamp <filename>
      
      
**Hint**: Putting timestamp.reg into your registry will extend the file explorer context menu by a menu item "Timestamp filename"
